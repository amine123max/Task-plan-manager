interface:
  display_name: "Task Plan Manager"
  short_description: "Plan substantive work and skip simple chat."
  default_prompt: "Use $task-plan-manager to plan multi-step work with update_plan, but skip a task list for simple requests."
policy:
  allow_implicit_invocation: true
